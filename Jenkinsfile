#!/bin/bash
pipeline {
    agent any 
        stages {
                stage('Deploy to NP3') 
                    {
                    steps {
                //sh 'python --version'
                    echo 'successfully deployed to NP3'
                        }
                    }
                }
        
//end pipeline
}


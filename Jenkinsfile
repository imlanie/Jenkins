#!/bin/bash
pipeline {
    agent any 
    stages {
        stage('build') {
            steps {
                //sh 'python --version'
                echo 'this is build step'
            }
        }
    }
}
